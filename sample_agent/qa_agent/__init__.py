"""
Tiny retrieval-augmented Q&A agent.

Second sample agent shipped alongside the Travel Planner so the README
can show Nengok pointed at more than one trace project. A single
injectable failure mode (`retriever`) drops the retrieved context on
the floor so Gemini answers from memory and hallucinates.
"""
